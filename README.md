# Project_Palirates
한국공대 2025년도 졸업작품


 C++ 방식 최대한 활용할 것
------------------------------------------------------------------------------------------------
- 객체를 shared_ptr로 관리
- 컨테이너 vector로 변경
- 문자열 string_view 활용


주의 사항
------------------------------------------------------------------------------------------------
- 사용할 조명이 많아질 경우, Deffered Lighting 최적화 할 것 // 테셀레이션을 이용하여, 조명 영역을 구하고, 영역에 포함되는 픽셀에서만 조명 연산하는 방식




현재 진행 내용:
===================================================================
- Scene_Manager
- OBB_Drawer
- Text_UI_Render
- Text_UI_Manager
- Tile_Map
- Particle_Manager 


작업 목표:
------------------------------------------------------------------------------------------------
포커스 블러, 객체별 모션 블러 적용하기

파티클에 모션 블러 적용하기

현재 애니메이션 캐릭터에 모션 블러 정상적으로 적용하려면 
-> 이전 프레임의 스켈레톤 행렬이 필요하다 == 이전 프레임의 스켈레톤 행렬을 저장하고 있어야 한다
-> 애니메이션 관리 구조 정리 필요



진행 상황:
------------------------------------------------------------------------------------------------
카운터 값에서 일어나는 언더플로우 현상 해결 -> 카운터 버퍼 Dispatch와 초기화 동작을 같은 커멘드리스트에서 동작했기 때문, 별도의 커멘드리스트로 분리

그러나 Append 가 제대로 카운팅 안되는 현상 지속됨

-> UAV카운터 사용 중단하고, 다른 방법으로 접근할 것
---> UAV로 구조체 하나 전달 == 동시에 바인딩하는 다른 StructuredBuffer들의 카운터 값을 hlsl에서 저장하고, CPU에서 읽는 용도

Particle_Info, FreeList, RenderInstance 버퍼에 대한 카운터 저장 구조체 만들기

=> Queue 방식으로 사용하던 FreeList에 대한 동작 변경 필요 & RenderInstance도 마찬가지 



입자가 생성되고, 제거되는지 확인 되면, 인스턴싱 데이터 추출 동작도 함께 완성되는 거임
-> 그러면 파티클 인스턴싱 렌더링 구현하기


할 일
------------------------------------------------------------------------------------------------

우선 순위 1:
새로운 파티클 시스템 완성하기
파티클은 인스턴싱 데이터에 속도 벡터를 추가해야 함


우선 순위 2:
DXGI_FORMAT_R16G16B16A16_FLOAT,   
- x,y : Velocity
- z: Blur Mask 
- w: Object ID == ID에 따라 윤곽선 색깔 선택 -> 플레이어 고유 색상 판별
G 버퍼 완성할 것





