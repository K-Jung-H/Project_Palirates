# Project_Palirates
한국공대 2025년도 졸업작품


 C++ 방식 최대한 활용할 것
------------------------------------------------------------------------------------------------
- 객체를 shared_ptr로 관리
- 컨테이너 vector로 변경
- 문자열 string_view 활용


주의 사항
------------------------------------------------------------------------------------------------
- 사용할 조명이 많아질 경우, Deffered Lighting 최적화 할 것 // 테셀레이션을 이용하여, 조명 영역을 구하고, 영역에 포함되는 픽셀에서만 조명 연산하는 방식




현재 진행 내용:
===================================================================
- Scene_Manager
- OBB_Drawer
- Text_UI_Render
- Text_UI_Manager
- Tile_Map
- Particle_Manager 


작업 목표:
------------------------------------------------------------------------------------------------
포커스 블러, 객체별 모션 블러 적용하기

파티클에 모션 블러 적용하기

현재 애니메이션 캐릭터에 모션 블러 정상적으로 적용하려면 
-> 이전 프레임의 스켈레톤 행렬이 필요하다 == 이전 프레임의 스켈레톤 행렬을 저장하고 있어야 한다
-> 애니메이션 관리 구조 정리 필요



진행 상황:
------------------------------------------------------------------------------------------------
파티클 객체 생명 주기 관리 및 업데이트를 기존 방식(GS, SO)에서 CS에서 모두 관리하도록 변경하는 중
사실상 파티클 파이프라인 다 뜯어 고치고 있음
-> 프레임워크 일부 재정리 // 각 커멘드 리스트 별로 이전 동작이 끝났는지 Fence 값 검토하는 기능 추가 및 리팩토링

과정에서 버퍼 생성 포맷 일부 변경 및 (단일)구조체 버퍼 관리 기능 추가 완료


CS로 전달한 파티클 인덱스 버퍼 크기 읽기 성공
-> 크기 읽기가 가능 == 인스턴싱 개수 설정 가능

카운터 버퍼 값 초기화 하는 함수 정의 완료 

문제점: FreeList의 카운터 버퍼 값이 이상하게 읽혀지는 중 -> 원인 찾기 필요

FreeList: 파티클을 관리하는 두 CS 함수에서 새로 생성/제거할 파티클의 인덱스를 관리하는 배열

입자가 생성되고, 제거되는지 확인 되면, 인스턴싱 데이터 추출 동작도 함께 완성되는 거임
-> 그러면 파티클 인스턴싱 렌더링 구현하기


할 일
------------------------------------------------------------------------------------------------

우선 순위 1:
새로운 파티클 시스템 완성하기
파티클은 인스턴싱 데이터에 속도 벡터를 추가해야 함


우선 순위 2:
DXGI_FORMAT_R16G16B16A16_FLOAT,   
- x,y : Velocity
- z: Blur Mask 
- w: Object ID == ID에 따라 윤곽선 색깔 선택 -> 플레이어 고유 색상 판별
G 버퍼 완성할 것





