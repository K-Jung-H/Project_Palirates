# Project_Palirates
한국공대 2025년도 졸업작품


 C++ 방식 최대한 활용할 것
------------------------------------------------------------------------------------------------
- 객체를 shared_ptr로 관리
- 컨테이너 vector로 변경
- 문자열 string_view 활용


주의 사항
------------------------------------------------------------------------------------------------
- 커밋 할때, 파일의 인코딩 형식 확인할 것
- UTF-16 계열인 경우, UTF-8로 변환 할 것 // 깃에서 파일이 바이너리로 읽히게 되는 원인

- 사용할 조명이 많아질 경우, Deffered Lighting 최적화 할 것 // 테셀레이션을 이용하여, 조명 영역을 구하고, 영역에 포함되는 픽셀에서만 조명 연산하는 방식

- Object::Render 에서 계층 구조 따라 갈때, Active에 의한 동작 전환이 이상해보임 - 검토 필요



사용법
------------------------------------------------------------------------------------------------
- 카메라 조종 방법: W/A/S/D
 - 카메라 토글: 1/2/3/4/5/6
- 씬 전환 : TAB

현재 진행 내용:
===================================================================
- Scene_Manager
- OBB_Drawer
- Text_UI_Render
- Text_UI_Manager
- Tile_Map
- Particle_Manager 
- Light_Material_Manager

- OBB_Render
- OBB_CollisionManager
- OBB_Manager

작업 목표:
------------------------------------------------------------------------------------------------
클라이언트에서는 키입력 및 플레이어의 마우스 회전 결과의 look 벡터 전달하기
서버에서는 키입력을 기반으로 플레이어 속력 업데이트 및 위치 업데이트 + 충돌처리까지 처리하기



진행 상황:
------------------------------------------------------------------------------------------------
줌-포커스 블러 추가중
- 블러 관련 데이터, 픽셀별 속도 벡터 를 저장 및 관리하는 G-Buffer 분리
- PostEffect_Manager에서 이제 필요한 리소스를 동시에 여러개 바인딩 예약 처리 할 수 있게 기능 추가
- 객체의 좌표를 스크린으로 변환해서 전달하는 것까지 체크 완료
-> 이제 해당 좌표를 향해서 각 줌 효과를 추가하면 됨


문제점:
- 기존에  화면 전환 시 일부 발생하던 오류 발생 시점 발견 -> 윤곽선 처리 과정에서 발생
	- 마지막으로 사용했던 프레임의 데이터를 계속 반복해서 리소스로 재사용하는 구조의 문제
	-> 후처리에 사용하는 각 UAV를, 다시 사용하기 전에 초기화 하는 동작 추가 필요

-> 문제점을 해결하려면, 서술자의 CPU 핸들 값이 필요함, 그러나 기존 구조는 GPU 핸들만 저장 중
	-> 이를 해결하기 위해, 기존 힙 구조에 CPU 핸들를 저장하는 변수를 추가했지만, 문제 발생 
						// 하나의 힙에서는 GPU 핸들, CPU 핸들을 동시에 관리하면 안됨

// 아니면, 빈 버퍼를 만들어서, 해당 내용을 복사하도록 하여, 강제로 초기화 하도록 해도 됨 -> 매우 비효율적

아무리 다시 생각해도 UAV를 초기화 안하더라도, 현재 렌더타겟 내용을 SRV로 참고하여, 윤곽선 처리하도록 되어 있음
== 이 문제는 리소스 자체에 문제가 생기고 있는거임

인게임으로 진입할때만 발생하는 문제인가? 
-> 애매함

씬 전환 동작과 CS 동작이 동시에 일어나서 발생하는 문제인가?
-> 모든 Commandlist Stage가 끝난 경우 전환하도록 해보았음 + Change_Scene 호출 위치를 계속 변화시켜 봄
--> 변화 없었음


# Board_Scene에서 파도 객체의 Set_DetailTexture를 설정 안하니까 오류 발생 안함 // 해당 코드의 유무에 따라 오류가 발생중 
-> DescriptorHeap에 SRV 공간이 부족한가? -> X, 공간을 더 할당하였으나, 문제가 지속됨
-> 해당 텍스쳐가 실제로 파일에 없는가? -> X, 파일은 제대로 된 주소에 존재함, 또한 BaseTexture에 시도하니, 제대로 동작함

+ 기본 객체들의 텍스쳐가 강제로 Detail에 덮어 씌워지고 있는 현상 발견
-> Detail 설정을 아예 안하고 동작할 경우, 검은색을 샘플링 하여 Base 색상에 검은색이 블랜딩 되야 함-> 그러나, 다른 기본 객체들의 텍스쳐를 샘플링 하여, 렌더링에 사용하는 결과 발생
-> Wave 객체의 바인딩 또는 텍스쳐 관리에 오류가 생겻을 것


그것과 윤곽선이 무슨 연관이 있는가?
-> 원인 파악 못함
-> 하지만, Wave를 비활성화 하면, 윤곽선이 정상 동작 함 // 코드도 개선하여 적용 해보았음



할 일
------------------------------------------------------------------------------------------------
줌-포커스 블러 추가
윤곽선 처리 과정에서 발생하는 오류 해결하기

서버에 충돌 검사 추가하기

